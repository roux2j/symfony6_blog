{% extends "theme/theme_front.html.twig" %}

{% block title %}Contact{% endblock %}

{% block main %}
{# <div class="col-sm-12">
    <div class="form-group">
{{ form_start(contactForm) }}
    {{ form_row(contactForm.nom, {'attr': {'class': 'form-control'}}) }}


</div>
<div class="form-group">
    {{ form_row(contactForm.email, {'attr': {'class': 'form-control'}}) }}

</div>
<div class="form-group">
    {{ form_row(contactForm.message, {'attr': {'class': 'form-control'}}) }}

</div>

<div class="form-group">
    <input type="submit" value="Send" class="btn btn-primary py-3 px-5">
</div>
{{ form_end(contactForm) }}
</div> #}

</div>


<h1 class="text-center display-5 mt-5 quicksand">Contact</h1>
<div class="heading-line"></div>

<div class="container my-3">
    <div class="row">
        <div class="col-md-7 mx-auto p-4 bg-white shadow">
            {{ form_start(contactForm) }}
            <div class="row">
                <div class="col-md-12">
                    <div class="mb-3">
                        {# <label for="registration_form_firstName">Prénom</label> #}
                        {# <div class="text-danger">{{ form_errors(contactForm.nom)}}</div> #}
                        {{ form_row(contactForm.nom, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="mb-3">
                        {# <label for="registration_form_email">Email</label> #}
                        {# <div class="text-danger">{{ form_errors(contactForm.email)}}</div> #}
                        {{ form_row(contactForm.email, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="mb-3">
                        {# <label for="registration_form_password_second">Confirmation du mot de passe</label> #}
                        {# <div class="text-danger">{{ form_errors(contactForm.message)}}</div> #}
                        {{ form_row(contactForm.message, {'attr': {'class': 'form-control'}}) }}
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <div class="mb-3 text-center">
                        <input formnovalidate type="submit" class="btn bg-light" value="envoyer" />
                    </div>
                </div>
            </div>
            {{ form_end(contactForm) }}
        </div>
    </div>
</div>
{% endblock %}